# How to run the dashboard 
After sourcing the workspace,   
To start the web server run `python "Laptop Dashboard"/start_dashboard.py`   
To start the pi dashboard run `ros2 run dashboard dashboard`    
OPTIONAL: For the camera stream if you have a realsense camera hooked up run `ros2 run dashboard camera`   
